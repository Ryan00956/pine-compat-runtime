//@version=5
strategy("Exit reservation qty percent stop multi")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP1", "L", stop=2.5, qty_percent=25)
    strategy.exit("XP2", "L", stop=2.25, qty_percent=50)
plot(strategy.position_size)
plot(strategy.netprofit)
