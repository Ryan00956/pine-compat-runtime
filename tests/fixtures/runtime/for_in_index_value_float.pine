//@version=5
indicator("For in index value float")

values = array.from(1.5, 2.5, 3.5)

int index_sum = 0
float weighted_sum = 0.0
for index, value in values
    index_sum := index_sum + index
    weighted_sum := weighted_sum + value * (index + 1)

plot(index_sum)
plot(weighted_sum)
