//@version=5
strategy("Active entry exit attachment", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, limit=2)
    strategy.exit("XL", "L", stop=2.5)

plot(strategy.position_size)
plot(strategy.closedtrades)
