//@version=5
strategy("Max position size does not bind strategy.order")
strategy.risk.max_position_size(1)
if bar_index == 0
    strategy.order("L", strategy.long, qty=5)
plot(strategy.position_size)
plot(strategy.max_contracts_held_long)
