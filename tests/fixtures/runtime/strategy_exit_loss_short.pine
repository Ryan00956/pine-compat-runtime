//@version=5
strategy("Loss exit short strategy", overlay=true)
if bar_index == 0
    strategy.entry("S", strategy.short, qty=2)
if bar_index == 1
    strategy.exit("XL", "S", loss=50)
plot(strategy.position_size)
