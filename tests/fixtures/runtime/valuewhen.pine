//@version=5
indicator("Value When")
last_close = ta.valuewhen(close > open, close, 0)
previous_index = ta.valuewhen(close > open, bar_index, 1)
last_flag = ta.valuewhen(close > open, high > close, 0)
plot(last_close)
plot(previous_index)
plot(na(last_flag) ? 0 : last_flag ? 1 : -1)
