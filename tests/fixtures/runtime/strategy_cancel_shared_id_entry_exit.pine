//@version=5
strategy("Cancel shared id pending entry and exit")
if bar_index == 0
    strategy.entry("X", strategy.long, qty=1, limit=3)
    strategy.exit("X", "X", stop=1)
    strategy.cancel("X")
plot(strategy.position_size)
plot(strategy.opentrades)
plot(strategy.closedtrades)
