//@version=5
strategy("Stop long order flattens short")
if bar_index == 0
    strategy.entry("S", strategy.short, qty=2)
if bar_index == 1
    strategy.order("O", strategy.long, qty=2, stop=3)
plot(strategy.position_size)
plot(strategy.closedtrades)
