//@version=5
strategy("Exit qty bracket partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XQ", "L", stop=0.5, limit=2.5, qty=0.5)
plot(strategy.position_size)
plot(strategy.netprofit)
