//@version=5
strategy("Exit reservation qty percent clamp")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP1", "L", limit=1.5, qty_percent=75)
    strategy.exit("XP2", "L", limit=1.75, qty_percent=150)
plot(strategy.position_size)
plot(strategy.netprofit)
