//@version=5
strategy("Max position size reduces limit entry")
strategy.risk.max_position_size(2)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=5, limit=2)
plot(strategy.position_size)
plot(strategy.max_contracts_held_long)
