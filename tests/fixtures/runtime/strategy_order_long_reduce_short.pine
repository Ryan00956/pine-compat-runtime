//@version=5
strategy("Market long order reduces short")
if bar_index == 0
    strategy.entry("S", strategy.short, qty=2)
if bar_index == 1
    strategy.order("O", strategy.long, qty=1)
plot(strategy.position_size)
plot(strategy.closedtrades)
