//@version=5
strategy("Stop limit entry strategy", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, stop=3, limit=4)

plot(strategy.position_size)
plot(strategy.position_avg_price)
