//@version=5
strategy("Exit qty percent full clamp")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=1)
    strategy.exit("XP", "L", limit=2.5, qty_percent=150)
plot(strategy.position_size)
plot(strategy.netprofit)
