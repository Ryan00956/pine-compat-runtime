//@version=5
strategy("Calc on order fills exit from average", calc_on_order_fills=true)

if strategy.position_size == 0
    strategy.entry("L", strategy.long, qty=1)
else
    strategy.exit("XL", "L", limit=strategy.position_avg_price * 2)

plot(strategy.position_size)
plot(strategy.opentrades)
