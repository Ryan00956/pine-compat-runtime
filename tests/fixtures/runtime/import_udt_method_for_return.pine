//@version=5
import user/udt/1 as lib
indicator("Imported UDT method for return")

p = lib.Point.new(close)
other = lib.Point.new(close + 40)
same = p.sameFor(1)
chosen = p.chooseFor(other, 1)
plot(same.x + chosen.x)
