//@version=5
strategy("Max intraday filled orders reset", pyramiding=3)
strategy.risk.max_intraday_filled_orders(1)
if bar_index == 0
    strategy.entry("A", strategy.long, qty=1)
if bar_index == 2
    strategy.entry("B", strategy.long, qty=1)
plot(strategy.position_size)
