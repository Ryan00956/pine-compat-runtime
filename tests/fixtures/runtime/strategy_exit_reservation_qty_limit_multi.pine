//@version=5
strategy("Exit reservation qty limit multi")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XL1", "L", limit=1.5, qty=0.75)
    strategy.exit("XL2", "L", limit=1.75, qty=0.5)
plot(strategy.position_size)
plot(strategy.netprofit)
