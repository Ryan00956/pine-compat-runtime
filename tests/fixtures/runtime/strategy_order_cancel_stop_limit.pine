//@version=5
strategy("Cancel stop-limit strategy order")
if bar_index == 0
    strategy.order("O", strategy.long, qty=2, stop=3, limit=2)
    strategy.cancel("O")
plot(strategy.position_size)
plot(strategy.position_avg_price)
