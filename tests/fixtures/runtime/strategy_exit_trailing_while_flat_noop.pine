//@version=5
strategy("Strategy exit trailing while flat no-op")
if bar_index == 0
    strategy.exit("XT", "L", trail_price=2.5, trail_offset=50)
plot(strategy.position_size)
plot(strategy.closedtrades)
