//@version=5
import user/udt/1 as lib
indicator("Imported UDT for expression")

p = for i = 0 to 2
    made = lib.Point.new(close + i)
    made

lib.Point typed = for i = 0 to 1
    p

p_prior = p[1]

plot(p.x + typed.x)
plot(p_prior.x)
