//@version=5
strategy("Exit qty stop partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XQ", "L", stop=2.5, qty=0.75)
plot(strategy.position_size)
plot(strategy.netprofit)
