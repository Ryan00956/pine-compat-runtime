//@version=5
strategy("Exit qty percent bracket partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP", "L", stop=0.5, limit=2.5, qty_percent=25)
plot(strategy.position_size)
plot(strategy.netprofit)
