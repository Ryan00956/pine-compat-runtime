//@version=5
strategy("Bracket stop limit stop fill short")
if bar_index == 0
    strategy.entry("S", strategy.short, qty=2)
    strategy.exit("XB", "S", stop=2.5, limit=0.5)
plot(strategy.position_size)
