//@version=5
indicator("Dynamic history scopes", max_bars_back=3)

previous(src, offset) =>
    src[offset]

loop_offset = 0
for i = 0 to 2
    loop_offset := i

plot(close[loop_offset])
plot(previous(close + open, loop_offset))
