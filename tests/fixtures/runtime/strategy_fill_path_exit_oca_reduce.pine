//@version=5
strategy("Fill path exit oca reduce", overlay=false, initial_capital=100000)

if bar_index == 0
    strategy.entry("EN", strategy.long, qty=2)
    strategy.exit("TP", "EN", limit=10.8, qty=1, oca_name="g")
    strategy.exit("SL", "EN", stop=8.2, oca_name="g")
