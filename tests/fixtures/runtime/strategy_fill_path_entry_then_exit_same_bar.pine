//@version=5
strategy("Fill path entry then exit same bar", overlay=false, pyramiding=2, initial_capital=100000)

if bar_index == 0
    strategy.entry("EN", strategy.long, qty=1, stop=10.8)
    strategy.exit("EX", "EN", stop=8.5)
