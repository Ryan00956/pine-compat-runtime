//@version=5
strategy("Margin capital held long", margin_long=50)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 3
    strategy.close("L")
plot(strategy.opentrades.capital_held)
