//@version=5
strategy("Fill path realtime trailing rollback", overlay=false, calc_on_every_tick=true, initial_capital=100000)

if bar_index == 0
    strategy.entry("EN", strategy.long, qty=1)
    strategy.exit("TR", "EN", trail_price=10.5, trail_offset=50)
