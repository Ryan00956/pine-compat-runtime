//@version=5
indicator("User type functions")
type Point
    float x
    float y

identity(p) => p
wrap(p) =>
    copy = p
    copy

p = Point.new(close, open)
same = identity(p)
wrapped = wrap(p)
plot(same.x - same.y)
plot(same.x + same.y)
plot(wrapped.x + wrapped.y + bar_index)
