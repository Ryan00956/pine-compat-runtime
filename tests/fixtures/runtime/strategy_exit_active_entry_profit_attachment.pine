//@version=5
strategy("Active entry profit attachment", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, limit=2)
    strategy.exit("XP", "L", profit=50)

plot(strategy.position_size)
plot(strategy.closedtrades)
