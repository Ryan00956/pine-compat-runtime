//@version=5
strategy("Active entry loss attachment", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, limit=3)
    strategy.exit("XL", "L", loss=100)

plot(strategy.position_size)
plot(strategy.closedtrades)
