//@version=5
strategy("Strategy entry metadata", overlay=true)
message = close > open ? "up" : "down"
if bar_index == 1
    strategy.entry("L", strategy.long, qty=2, comment="entry", alert_message=message, disable_alert=false)
if bar_index == 3
    strategy.close("L")
plot(strategy.closedtrades)
