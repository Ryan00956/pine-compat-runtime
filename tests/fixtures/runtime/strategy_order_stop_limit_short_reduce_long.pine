//@version=5
strategy("Stop-limit short order reduces long")
if bar_index == 0
    strategy.entry("E", strategy.long, qty=2)
if bar_index == 1
    strategy.order("O", strategy.short, qty=1, stop=3, limit=4)
plot(strategy.position_size)
plot(strategy.closedtrades)
