//@version=5
strategy("Stop long strategy order")
if bar_index == 0
    strategy.entry("E", strategy.long, qty=1)
if bar_index == 1
    strategy.order("O", strategy.long, qty=2, stop=12)
plot(strategy.position_size)
plot(strategy.position_avg_price)
