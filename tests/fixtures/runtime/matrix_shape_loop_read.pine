//@version=6
indicator("Matrix shape loop read")

values = matrix.new<float>(2, 3, close)

shape_total = close * 0
for i = 0 to 1
    shape_total := shape_total + (i == 0 ? matrix.rows(values) : values.columns())

plot(shape_total, "shape_total")
