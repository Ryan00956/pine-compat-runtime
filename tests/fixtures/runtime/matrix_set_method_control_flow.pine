//@version=6
indicator("Matrix set method control flow")

values = matrix.new<float>(1, 2, 0)

for i = 0 to 1
    values.set(0, i, close + i)

plot(values.get(0, 0), "first")
plot(matrix.get(values, 0, 1), "second")
