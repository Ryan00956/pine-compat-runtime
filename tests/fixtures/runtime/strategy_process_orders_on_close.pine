//@version=5
strategy("Process orders on close", process_orders_on_close=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)

plot(strategy.position_size)
plot(strategy.opentrades)
