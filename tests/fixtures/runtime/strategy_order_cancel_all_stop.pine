//@version=5
strategy("Cancel all stop strategy order")
if bar_index == 0
    strategy.order("O", strategy.long, qty=2, stop=3)
    strategy.cancel_all()
plot(strategy.position_size)
plot(strategy.position_avg_price)
