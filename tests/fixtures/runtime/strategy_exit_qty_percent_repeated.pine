//@version=5
strategy("Exit qty percent repeated")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP", "L", limit=2, qty_percent=25)
if bar_index == 1
    strategy.exit("XP", "L", limit=2, qty_percent=25)
plot(strategy.position_size)
plot(strategy.netprofit)
