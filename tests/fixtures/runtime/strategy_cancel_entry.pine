//@version=5
strategy("Cancel entry strategy", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, limit=2)
    strategy.cancel("L")

plot(strategy.position_size)
plot(strategy.position_avg_price)
