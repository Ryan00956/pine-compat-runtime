//@version=5
strategy("Bracket repeated")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XB", "L", stop=0.5, limit=2)
if bar_index == 1
    strategy.exit("XB", "L", stop=0.5, limit=2)
plot(strategy.position_size)
