//@version=5
strategy("Strategy exit bracket unmatched from_entry no-op")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 1
    strategy.exit("XB", "OTHER", stop=low, limit=high)
plot(strategy.position_size)
plot(strategy.closedtrades)
