//@version=5
strategy("Trailing price fill short")
if bar_index == 0
    strategy.entry("S", strategy.short, qty=2)
    strategy.exit("XT", "S", trail_price=9.5, trail_offset=50)
plot(strategy.position_size)
plot(strategy.netprofit)
