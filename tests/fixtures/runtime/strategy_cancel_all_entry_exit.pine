//@version=5
strategy("Cancel all entry exit strategy", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, limit=2)
    strategy.exit("XL", "L", stop=1.5)
    strategy.cancel_all()

plot(strategy.position_size)
plot(strategy.closedtrades)
