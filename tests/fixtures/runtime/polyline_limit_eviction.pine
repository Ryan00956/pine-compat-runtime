//@version=5
indicator("Polyline limit eviction", max_polylines_count=2)

points = array.new<chart.point>()
array.push(points, chart.point.from_index(bar_index, close))

if bar_index == 0
    for i = 0 to 2
        polyline.new(points, line_color=color.red)

plot(array.size(polyline.all))
