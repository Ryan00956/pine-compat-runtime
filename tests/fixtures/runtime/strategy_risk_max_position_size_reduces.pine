//@version=5
strategy("Max position size reduces entry")
strategy.risk.max_position_size(2)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=5)
plot(strategy.position_size)
plot(strategy.max_contracts_held_long)
