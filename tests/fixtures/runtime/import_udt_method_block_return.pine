//@version=5
import user/udt/1 as lib
indicator("Imported UDT method block return")

p = lib.Point.new(close)
other = lib.Point.new(close + 20)
same = p.sameBlock()
chosen = p.chooseBlock(other)
plot(same.x + chosen.x)
