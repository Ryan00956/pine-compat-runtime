//@version=5
strategy("Strategy exit stop profit bracket while flat no-op")
if bar_index == 0
    strategy.exit("XB", "L", stop=low, profit=1)
plot(strategy.position_size)
plot(strategy.closedtrades)
