//@version=5
strategy("Exit reservation qty mixed stop multi")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XM1", "L", stop=2.5, qty=0.75)
    strategy.exit("XM2", "L", stop=2.25, qty_percent=50)
plot(strategy.position_size)
plot(strategy.netprofit)
