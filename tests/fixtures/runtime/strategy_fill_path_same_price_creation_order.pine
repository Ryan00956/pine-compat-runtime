//@version=5
strategy("Fill path same price creation order", overlay=false, pyramiding=2, initial_capital=100000)

if bar_index == 0
    strategy.entry("LIM", strategy.short, qty=1, limit=10.8)
    strategy.entry("STP", strategy.long, qty=1, stop=10.8)
