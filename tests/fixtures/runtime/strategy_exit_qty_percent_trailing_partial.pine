//@version=5
strategy("Exit qty percent trailing partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP", "L", trail_points=150, trail_offset=50, qty_percent=25)
plot(strategy.position_size)
plot(strategy.netprofit)
