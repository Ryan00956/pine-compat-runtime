//@version=5
indicator("Color outputs")
if close > 1
    bgcolor(color.green, title="Background", offset=0, editable=false, show_last=5, display=display.data_window)
barcolor(close > 2 ? color.red : na, title="Bars", offset=0, editable=true, show_last=5, display=display.none)
plot(close)
