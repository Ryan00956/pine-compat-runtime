//@version=5
indicator("Local Scope")
if close > open
    spread = high - low
    [hi, lo] = [high, low]
    var seen = 10
    seen := seen + 1
    plot(seen + spread + hi - lo)
total = 0
for i = 0 to 2
    var count = 0
    count := count + 1
    total := count
plot(total)
