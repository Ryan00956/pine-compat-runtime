//@version=5
strategy("Fill path exit then entry same bar", overlay=false, pyramiding=2, initial_capital=100000)

if bar_index == 0
    strategy.entry("EN", strategy.long, qty=1)
if bar_index == 1
    strategy.exit("EX", "EN", limit=10.8)
    strategy.entry("NX", strategy.long, qty=1, limit=8.2)
