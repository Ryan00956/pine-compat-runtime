//@version=5
indicator("Switch statement block loop control fixture")
total = 0

for i = 0 to 5
    switch
        i == 1 =>
            total := total + 10
            continue
            0
        i == 4 =>
            total := total + 100
            break
            0
        =>
            total := total + i
            0

plot(close + total)
