//@version=5
indicator("If")
x = close
if close > open
    x := close
else
    x := open
plot(x)
branch_value = if close > open
    spread = high - low
    spread + close
else
    open + close
plot(branch_value)
