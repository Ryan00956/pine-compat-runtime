//@version=5
strategy("Strategy exit unmatched from_entry no-op")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 1
    strategy.exit("XL", "OTHER", stop=low)
plot(strategy.position_size)
plot(strategy.closedtrades)
