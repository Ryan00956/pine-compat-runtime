//@version=5
strategy("Exit qty trailing partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XQ", "L", trail_points=150, trail_offset=50, qty=0.5)
plot(strategy.position_size)
plot(strategy.netprofit)
