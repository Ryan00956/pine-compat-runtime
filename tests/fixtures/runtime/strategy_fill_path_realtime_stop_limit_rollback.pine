//@version=5
strategy("Fill path realtime stop limit rollback", overlay=false, calc_on_every_tick=true, initial_capital=100000)

if bar_index == 0
    strategy.entry("SL", strategy.long, qty=1, stop=10.8, limit=8.2)
