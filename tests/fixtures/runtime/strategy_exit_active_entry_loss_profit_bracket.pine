//@version=5
strategy("Active entry loss profit bracket", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2, limit=2)
    strategy.exit("XB", "L", loss=100, profit=150)

plot(strategy.position_size)
plot(strategy.closedtrades)
