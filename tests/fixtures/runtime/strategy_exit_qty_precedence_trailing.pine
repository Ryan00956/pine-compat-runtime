//@version=5
strategy("Exit qty precedence trailing")

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)

if bar_index == 1
    strategy.exit("TQ", "L", trail_points=100, trail_offset=50, qty=0.75, qty_percent=25)

plot(strategy.position_size)
plot(strategy.closedtrades)
