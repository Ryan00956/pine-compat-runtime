//@version=5
strategy("Exit omitted single replacement")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XS", "L", stop=0.5)
    strategy.exit("XL", "L", limit=2.5)
plot(strategy.position_size)
plot(strategy.netprofit)
