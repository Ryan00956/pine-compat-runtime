//@version=5
strategy("Exit qty percent limit partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP", "L", limit=2.5, qty_percent=50)
plot(strategy.position_size)
plot(strategy.netprofit)
