//@version=5
indicator("Alert frequency fixture")
if bar_index == 0
    for i = 0 to 1
        alert("Default once")
    for i = 0 to 1
        alert("Explicit once", alert.freq_once_per_bar)
    for i = 0 to 1
        alert("All", alert.freq_all)
