//@version=5
strategy("Strategy exit stop profit bracket unmatched from_entry no-op")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 1
    strategy.exit("XB", "OTHER", stop=low, profit=1)
plot(strategy.position_size)
plot(strategy.closedtrades)
