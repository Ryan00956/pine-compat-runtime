//@version=5
strategy("Fill path margin before exit long", overlay=false, initial_capital=120, margin_long=25)

if bar_index == 0
    strategy.entry("EN", strategy.long, qty=40)
    strategy.exit("EX", "EN", limit=10.8)
