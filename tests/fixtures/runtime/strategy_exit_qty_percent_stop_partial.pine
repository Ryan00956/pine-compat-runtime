//@version=5
strategy("Exit qty percent stop partial")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XP", "L", stop=2.5, qty_percent=50)
plot(strategy.position_size)
plot(strategy.netprofit)
