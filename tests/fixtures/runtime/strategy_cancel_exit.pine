//@version=5
strategy("Cancel exit strategy", overlay=true)

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)

if bar_index == 1
    strategy.exit("XL", "L", limit=4)
    strategy.cancel("XL")

plot(strategy.position_size)
