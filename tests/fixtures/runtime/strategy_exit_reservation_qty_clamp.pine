//@version=5
strategy("Exit reservation qty clamp")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
    strategy.exit("XC1", "L", limit=1.5, qty=1.5)
    strategy.exit("XC2", "L", limit=1.75, qty=1)
plot(strategy.position_size)
plot(strategy.netprofit)
