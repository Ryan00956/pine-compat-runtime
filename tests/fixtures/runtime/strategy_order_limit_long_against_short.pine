//@version=5
strategy("Limit long order crosses zero against short")
if bar_index == 0
    strategy.entry("S", strategy.short, qty=1)
if bar_index == 1
    strategy.order("O", strategy.long, qty=2, limit=3)
plot(strategy.position_size)
plot(strategy.closedtrades)
