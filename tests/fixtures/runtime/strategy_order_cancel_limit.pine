//@version=5
strategy("Cancel limit strategy order")
if bar_index == 0
    strategy.order("O", strategy.long, qty=2, limit=2)
    strategy.cancel("O")
plot(strategy.position_size)
plot(strategy.position_avg_price)
