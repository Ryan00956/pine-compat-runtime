//@version=5
strategy("Close all immediately")

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 2
    strategy.close_all(immediately=true)

plot(strategy.position_size)
plot(strategy.closedtrades)
plot(strategy.opentrades)
