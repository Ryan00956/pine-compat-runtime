//@version=5
strategy("Stop-limit entry reverses long")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=1)
if bar_index == 1
    strategy.entry("S", strategy.short, qty=1, stop=3, limit=4)
plot(strategy.position_size)
plot(strategy.closedtrades)
plot(strategy.opentrades)
