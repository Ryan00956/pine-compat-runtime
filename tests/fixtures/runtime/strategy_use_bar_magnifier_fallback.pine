//@version=6
strategy("Bar magnifier fallback", overlay=false, use_bar_magnifier=true, initial_capital=100000)

if bar_index == 0
    strategy.entry("EN", strategy.long, qty=1)

plot(strategy.position_size)
