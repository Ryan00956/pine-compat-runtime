//@version=5
strategy("Fill path limit stop collision", pyramiding=2)

if bar_index == 0
    strategy.entry("LIM", strategy.long, qty=1, limit=2)
    strategy.entry("STP", strategy.long, qty=1, stop=2)

plot(strategy.opentrades)
plot(strategy.position_size)
