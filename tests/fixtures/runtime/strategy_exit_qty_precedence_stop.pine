//@version=5
strategy("Exit qty precedence stop")

if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)

if bar_index == 1
    strategy.exit("XQ", "L", stop=3, qty=0.75, qty_percent=25)

plot(strategy.position_size)
plot(strategy.closedtrades)
