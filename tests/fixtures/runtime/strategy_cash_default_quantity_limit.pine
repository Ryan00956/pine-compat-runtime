//@version=5
strategy("Strategy cash default quantity limit", initial_capital=1000, default_qty_type=strategy.cash, default_qty_value=100)
if bar_index == 0
    strategy.entry("D", strategy.long, limit=2)
plot(strategy.position_size)
plot(strategy.position_avg_price)
