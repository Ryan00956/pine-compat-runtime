//@version=5
strategy("Trailing points fill short")
if bar_index == 0
    strategy.entry("S", strategy.short, qty=2)
if bar_index == 1
    strategy.exit("XT", "S", trail_points=50, trail_offset=50)
plot(strategy.position_size)
plot(strategy.netprofit)
