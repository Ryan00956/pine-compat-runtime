//@version=5
strategy("Stop-limit short order crosses zero against long")
if bar_index == 0
    strategy.entry("E", strategy.long, qty=1)
if bar_index == 1
    strategy.order("O", strategy.short, qty=2, stop=3, limit=4)
plot(strategy.position_size)
plot(strategy.closedtrades)
