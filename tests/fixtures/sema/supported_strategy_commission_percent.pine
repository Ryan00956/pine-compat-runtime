//@version=5
strategy("Commission percent", commission_type=strategy.commission.percent, commission_value=10)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 2
    strategy.close("L")
plot(strategy.closedtrades.commission(0))
