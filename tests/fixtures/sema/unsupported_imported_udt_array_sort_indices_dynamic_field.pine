//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array sort_indices dynamic field")

field = close > open ? "value" : "missing"
values = array.from(lib.Point.new(close))
indices = array.sort_indices(values, order.ascending, field)
plot(close)
