//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.includes mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
plot(array.includes(points, Point.new(high)) ? 1 : 0)
