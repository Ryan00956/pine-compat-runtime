//@version=6
indicator("Supported empty matrix.pinv for-in result negative body history")
source = matrix.new<float>(3, 0, 0)
values = matrix.pinv(source)
length = for row in values
    -1
plot(close[length])
