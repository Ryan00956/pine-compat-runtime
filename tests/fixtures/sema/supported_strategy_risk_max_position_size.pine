//@version=5
strategy("Supported strategy risk max_position_size")
limit = 2
strategy.risk.max_position_size(2)
strategy.risk.max_position_size(limit)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=5)
