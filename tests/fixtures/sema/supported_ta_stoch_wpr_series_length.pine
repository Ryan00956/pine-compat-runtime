//@version=5
indicator("Supported ta.stoch/wpr series length")
length = bar_index % 2 + 2
plot(ta.stoch(close, high, low, length))
plot(ta.stoch(source=close, high=high, low=low, length=length))
plot(ta.wpr(length))
plot(ta.wpr(length=length))
