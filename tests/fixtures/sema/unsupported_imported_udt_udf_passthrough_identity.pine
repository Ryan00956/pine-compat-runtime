//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT UDF passthrough identity")

type Point
    float x

passthrough(p) => p

lib.Point p = passthrough(Point.new(close))
plot(p.x)
