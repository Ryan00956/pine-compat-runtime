//@version=5
indicator("Unsupported array.from nested UDT field")
type Point
    float x
    float y

type Wrapper
    Point inner
    float weight

values = array.from(Wrapper.new(Point.new(close, open), high))
plot(close)
