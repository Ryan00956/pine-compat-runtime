//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.concat local target method mixed identity")

type Point
    float x

local_points = array.from(Point.new(close))
imported_points = array.from(lib.Point.new(high))
result = local_points.concat(imported_points)
plot(close)
