//@version=6
indicator("Unsupported for-in array.slice named result negative history")
source = array.new<int>(2, 0)
values = array.slice(id = source, index_from = 0, index_to = 1)
length = for value in values
    -1
plot(close[length])
