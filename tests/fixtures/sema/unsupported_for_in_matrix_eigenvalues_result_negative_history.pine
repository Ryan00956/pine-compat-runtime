//@version=6
indicator("Unsupported for-in matrix.eigenvalues result negative history")
source = matrix.new<float>(2, 2, 1.0)
values = matrix.eigenvalues(source)
length = for value in values
    -1
plot(close[length])
