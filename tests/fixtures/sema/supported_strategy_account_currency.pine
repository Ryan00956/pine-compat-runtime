//@version=5
strategy("Supported strategy account currency", currency=currency.NONE)
currency_prefix = syminfo.prefix(strategy.account_currency)
plot(str.length(currency_prefix))
plot(strategy.account_currency == "USD" ? 1 : 0)
plot(strategy.account_currency[1] == "USD" ? 1 : 0)
