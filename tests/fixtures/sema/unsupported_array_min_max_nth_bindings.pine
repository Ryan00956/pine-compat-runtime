//@version=6
indicator("Unsupported array.min/max nth bindings")

ints = array.from(1, 2, 3)
floats = array.from(1.0, 2.0, 3.0)

plot(array.min(nth=1))
plot(array.max(id=ints, id=floats))
plot(array.min(ints, id=floats))
plot(array.max(nth=1, ints))
plot(ints.min(id=floats))
