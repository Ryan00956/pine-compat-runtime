//@version=5
indicator("Unsupported for statement reassignment series bound qualifier")

length = input.int(2, "Length")
selected = length
for i = 0 to bar_index
    selected := length

plot(ta.ema(close, selected))
