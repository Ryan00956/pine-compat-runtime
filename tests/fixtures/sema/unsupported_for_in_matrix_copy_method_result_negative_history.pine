//@version=6
indicator("Unsupported for-in matrix.copy method result negative history")
source = matrix.new<float>(1, 1, 0)
values = source.copy()
length = for row in values
    -1
plot(close[length])
