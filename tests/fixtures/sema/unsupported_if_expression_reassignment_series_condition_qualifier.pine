//@version=5
indicator("Unsupported if expression reassignment series condition qualifier")

length = input.int(2, "Length")
selected = length
_ = if close > open
    selected := length
    1
else
    0

plot(ta.ema(close, selected))
