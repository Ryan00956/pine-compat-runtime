//@version=6
indicator("Unsupported for-in matrix.inv result negative history")
source = matrix.new<float>(1, 1, 1.0)
values = matrix.inv(source)
length = for row in values
    -1
plot(close[length])
