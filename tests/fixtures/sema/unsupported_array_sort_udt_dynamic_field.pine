//@version=5
indicator("Unsupported array sort UDT dynamic field")
type Point
    float x

field = close > open ? "x" : "missing"
values = array.from(Point.new(close))
array.sort(values, order.ascending, field)
plot(close)
