//@version=6
indicator("Unsupported for-in matrix.diff left scalar result negative history")
right = matrix.new<float>(1, 0, 0)
values = matrix.diff(close, right)
length = for row in values
    -1
plot(close[length])
