//@version=6
indicator("Supported matrix add_col")

values = matrix.new<float>(2, 1, 1)
col_values = array.from(2.0, 3.0)
matrix.add_col(values, 1, col_values)
values.add_col(2, array.from(4.0, 5.0))

plot(matrix.rows(values) + matrix.columns(values))
