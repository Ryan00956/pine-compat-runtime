//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.lastindexof mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
plot(array.lastindexof(points, Point.new(high)))
