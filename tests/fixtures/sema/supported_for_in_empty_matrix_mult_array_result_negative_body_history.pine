//@version=6
indicator("Supported empty matrix.mult array result negative body history")
left = matrix.new<float>(0, 2, 0)
values = matrix.mult(left, array.from(1, 2))
length = for value in values
    -1
plot(close[length])
