//@version=6
indicator("Unsupported for-in matrix.pow named result negative history")
source = matrix.new<float>(1, 1, 1)
values = matrix.pow(id = source, power = 2)
length = for row in values
    -1
plot(close[length])
