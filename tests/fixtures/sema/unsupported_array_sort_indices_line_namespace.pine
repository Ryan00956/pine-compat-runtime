//@version=5
indicator("Unsupported array sort_indices line namespace")
values = array.new_line()
values.push(line.new(bar_index, low, bar_index + 1, high))
indices = array.sort_indices(values)
plot(close)
