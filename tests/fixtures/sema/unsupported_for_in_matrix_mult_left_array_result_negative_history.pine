//@version=6
indicator("Unsupported for-in matrix.mult left array result negative history")
right = matrix.new<float>(2, 1, 0)
values = matrix.mult(array.from(1, 2), right)
length = for value in values
    -1
plot(close[length])
