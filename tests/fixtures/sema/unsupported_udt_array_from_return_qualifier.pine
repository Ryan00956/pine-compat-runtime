//@version=6
indicator("Unsupported UDT array.from return qualifier")

type Point
    float x

type Other
    float x

first_x(array<Point> values) =>
    point = array.get(values, 0)
    point.x

others = array.from(Other.new(close))
plot(first_x(others))
plot(first_x(array.from(Other.new(open))))
