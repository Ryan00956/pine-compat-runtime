//@version=6
indicator("Unsupported branch local alias switch negative history")
use_short = input.bool(true, "Use short")
length = switch
    use_short =>
        base = -1
        base
    =>
        base = -1
        base
plot(close[length])
