//@version=6
indicator("Unsupported named const numeric if comparison ternary negative history")
fast = if true
    3.0
else
    2.0
use_short = fast > 2.0
length = use_short ? -1 : 1
plot(close[length])
