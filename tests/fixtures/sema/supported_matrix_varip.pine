//@version=6
indicator("Supported matrix varip")

varip values = matrix.new<float>(1, 1, close)
current = values.get(0, 0)
values.set(0, 0, current + close)

varip ints = matrix.new<int>(1, 1, 0)
ints.set(0, 0, bar_index)

plot(values.get(0, 0) + ints.get(0, 0))
