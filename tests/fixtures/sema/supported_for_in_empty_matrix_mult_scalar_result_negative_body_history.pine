//@version=6
indicator("Supported empty matrix.mult scalar for-in result negative body history")
left = matrix.new<float>(0, 2, 0)
values = matrix.mult(left, close)
length = for row in values
    -1
plot(close[length])
