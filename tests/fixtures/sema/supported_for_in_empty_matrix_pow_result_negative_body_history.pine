//@version=6
indicator("Supported empty matrix.pow for-in result negative body history")
source = matrix.new<float>(0, 0, 0)
values = matrix.pow(source, 2)
length = for row in values
    -1
plot(close[length])
