//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.push method mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
points.push(Point.new(high))
plot(close)
