//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.fill local target method mixed identity")

type Point
    float x

points = array.from(Point.new(close))
points.fill(lib.Point.new(high))
plot(close)
