//@version=6
indicator("Unsupported matrix.sort in UDF")

order_values(values) =>
    matrix.sort(values, 0)
    values

values = matrix.new<float>(1, 1, close)
plot(matrix.get(order_values(values), 0, 0))
