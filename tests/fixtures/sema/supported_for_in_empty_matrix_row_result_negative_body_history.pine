//@version=6
indicator("Supported empty matrix.row for-in result negative body history")
source = matrix.new<float>(1, 0, 0)
values = matrix.row(source, 0)
length = for value in values
    -1
plot(close[length])
