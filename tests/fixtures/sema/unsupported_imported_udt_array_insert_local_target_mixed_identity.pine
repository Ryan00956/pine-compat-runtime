//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.insert local target mixed identity")

type Point
    float x

points = array.from(Point.new(close))
array.insert(points, 0, lib.Point.new(high))
plot(close)
