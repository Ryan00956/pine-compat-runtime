//@version=5
indicator("Unsupported table.cell named const text size")
id = table.new(position.top_left, 1, 1)
size_base = "size.massive"
text_size = size_base
table.cell(id, 0, 0, "A", text_size=text_size)
