//@version=5
indicator("Unsupported array sort_indices UDT named const unknown field")
type Point
    float x

values = array.from(Point.new(close))
field_base = "missing"
field = field_base
indices = array.sort_indices(values, order.ascending, field)
plot(close)
