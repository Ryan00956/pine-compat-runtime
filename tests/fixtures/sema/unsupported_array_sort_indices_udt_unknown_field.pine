//@version=5
indicator("Unsupported array sort_indices UDT unknown field")
type Point
    float x

values = array.from(Point.new(close))
indices = array.sort_indices(values, order.ascending, "missing")
plot(close)
