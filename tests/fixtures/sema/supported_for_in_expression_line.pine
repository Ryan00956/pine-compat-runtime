//@version=6
indicator("Supported for-in expression line")

values = array.from(line.new(bar_index, high, bar_index + 1, low), line.new(bar_index + 2, high + 1, bar_index + 3, low + 1))
result = for value in values
    value

plot(line.get_x1(result) == bar_index + 2 ? 1 : 0)
