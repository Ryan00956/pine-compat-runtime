//@version=6
indicator("Supported empty matrix.mult left scalar for-in result negative body history")
right = matrix.new<float>(0, 2, 0)
values = matrix.mult(close, right)
length = for row in values
    -1
plot(close[length])
