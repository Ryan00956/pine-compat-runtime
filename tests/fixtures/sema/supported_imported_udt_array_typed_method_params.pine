//@version=5
import user/udt/1 as lib
indicator("Supported imported UDT array typed method params")

anchor = lib.Point.new(close)
points = array.from(lib.Point.new(close + 1), lib.Point.new(close + 2))
first = anchor.firstFrom(points)
second = lib.secondFrom(anchor, points)
plot(first.x + second.x)
