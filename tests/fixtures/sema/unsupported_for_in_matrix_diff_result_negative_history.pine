//@version=6
indicator("Unsupported for-in matrix.diff result negative history")
left = matrix.new<float>(1, 0, 0)
right = matrix.new<float>(1, 0, 0)
values = matrix.diff(left, right)
length = for row in values
    -1
plot(close[length])
