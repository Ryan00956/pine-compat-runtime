//@version=6
indicator("Unsupported for-in matrix.pinv method result negative history")
source = matrix.new<float>(0, 3, 0)
values = source.pinv()
length = for row in values
    -1
plot(close[length])
