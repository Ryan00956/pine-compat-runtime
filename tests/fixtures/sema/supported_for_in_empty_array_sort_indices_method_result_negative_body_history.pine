//@version=6
indicator("Supported empty array.sort_indices method for-in result negative body history")
source = array.new_float(0)
values = source.sort_indices()
length = for value in values
    -1
plot(close[length])
