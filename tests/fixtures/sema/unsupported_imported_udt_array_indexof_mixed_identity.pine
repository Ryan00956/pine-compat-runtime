//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.indexof mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
plot(array.indexof(points, Point.new(high)))
