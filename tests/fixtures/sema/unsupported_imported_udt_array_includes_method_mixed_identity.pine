//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.includes method mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
plot(points.includes(Point.new(high)) ? 1 : 0)
