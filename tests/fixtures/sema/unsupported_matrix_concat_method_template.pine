//@version=6
indicator("Unsupported matrix concat method template")

float_values = matrix.new<float>(1, 1, 1)
int_values = matrix.new<int>(1, 1, 1)
value = float_values.concat(int_values)
plot(value.rows())
