//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.concat local target mixed identity")

type Point
    float x

local_points = array.from(Point.new(close))
imported_points = array.from(lib.Point.new(high))
result = array.concat(local_points, imported_points)
plot(close)
