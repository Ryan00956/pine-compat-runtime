//@version=6
indicator("Supported matrix.submatrix")

values = matrix.new<float>(3, 4, close)
slice = matrix.submatrix(values, 0, 2, 1, 3)
copy = values.submatrix()
columns = values.submatrix(0, 3, 1)
plot(matrix.rows(slice) + matrix.columns(copy) + matrix.columns(columns))
