//@version=6
indicator("Unsupported for-in array.sort_indices method result negative history")
source = array.from(2.0, 1.0)
values = source.sort_indices()
length = for value in values
    -1
plot(close[length])
