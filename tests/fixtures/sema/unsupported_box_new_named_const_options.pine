//@version=5
indicator("Unsupported box.new named const options")
border_base = line.style_arrow_left
border_mode = border_base
format_base = text.format_bold
box_format = format_base + 4
box.new(bar_index, high, bar_index + 1, low, border_style=border_mode, text_formatting=box_format)
plot(close)
