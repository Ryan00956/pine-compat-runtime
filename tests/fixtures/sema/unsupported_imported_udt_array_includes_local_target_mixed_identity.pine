//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.includes local target mixed identity")

type Point
    float x

points = array.from(Point.new(close))
plot(array.includes(points, lib.Point.new(high)) ? 1 : 0)
