//@version=6
indicator("Supported for-in empty array.slice result negative body history")
source = array.new<int>(2, 0)
values = array.slice(source, 1, 1)
length = for value in values
    -1
plot(close[length])
