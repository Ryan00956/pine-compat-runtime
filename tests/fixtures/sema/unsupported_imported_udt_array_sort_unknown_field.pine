//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array sort unknown field")

values = array.from(lib.Point.new(close))
array.sort(values, order.ascending, "missing")
plot(close)
