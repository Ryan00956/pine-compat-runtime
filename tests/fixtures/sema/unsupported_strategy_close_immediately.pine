//@version=5
strategy("Unsupported strategy.close series immediately")
strategy.entry("L", strategy.long, qty=1)
strategy.close("L", immediately=close > open)
strategy.close_all(immediately=bar_index == 0)
