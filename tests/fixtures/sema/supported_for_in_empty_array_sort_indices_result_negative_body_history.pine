//@version=6
indicator("Supported empty array.sort_indices for-in result negative body history")
source = array.new_float(0)
values = array.sort_indices(source)
length = for value in values
    -1
plot(close[length])
