//@version=5
indicator("Unsupported ticker series simple numeric params")
kagi = ticker.kagi(syminfo.tickerid, "ATR", close)
pnf = ticker.pointfigure(syminfo.tickerid, "hl", "ATR", close, 3)
renko = ticker.renko(syminfo.tickerid, "ATR", close)
plot(close)
