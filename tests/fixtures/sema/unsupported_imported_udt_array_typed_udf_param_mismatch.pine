//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array typed UDF param mismatch")

type Point
    float x

points = array.from(Point.new(close))
first = lib.firstPoint(points)
plot(first.x)
