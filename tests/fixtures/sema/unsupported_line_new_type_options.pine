//@version=5
indicator("Unsupported line.new type options")
line_style = close > open ? "line.style_solid" : "line.style_dotted"
line.new(bar_index, low, bar_index + 1, high, style=line_style, width=close)
plot(close)
