//@version=6
indicator("Supported empty matrix.mult array method result negative body history")
left = matrix.new<float>(0, 2, 0)
values = left.mult(array.from(1, 2))
length = for value in values
    -1
plot(close[length])
