//@version=6
indicator("Unsupported for-in matrix.col named result negative history")
source = matrix.new<float>(2, 1, 0)
values = matrix.col(id = source, column = 0)
length = for value in values
    -1
plot(close[length])
