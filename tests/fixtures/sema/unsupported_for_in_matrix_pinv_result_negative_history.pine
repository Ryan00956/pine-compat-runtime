//@version=6
indicator("Unsupported for-in matrix.pinv result negative history")
source = matrix.new<float>(0, 3, 0)
values = matrix.pinv(source)
length = for row in values
    -1
plot(close[length])
