//@version=6
indicator("Unsupported for-in matrix.kron named result negative history")
left = matrix.new<float>(1, 0, 0)
right = matrix.new<float>(2, 3, 0)
values = matrix.kron(id1 = left, id2 = right)
length = for row in values
    -1
plot(close[length])
