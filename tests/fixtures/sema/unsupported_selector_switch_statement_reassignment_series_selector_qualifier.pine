//@version=5
indicator("Unsupported selector switch statement reassignment series selector qualifier")

length = input.int(2, "Length")
selected = length
switch bar_index
    0 =>
        selected := length

plot(ta.ema(close, selected))
