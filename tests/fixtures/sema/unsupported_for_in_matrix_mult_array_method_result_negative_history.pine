//@version=6
indicator("Unsupported for-in matrix.mult array method result negative history")
left = matrix.new<float>(1, 2, 0)
values = left.mult(array.from(1, 2))
length = for value in values
    -1
plot(close[length])
