//@version=6
indicator("Unsupported builtin map.copy call-result reads")

source = map.new<string, float>()

wrong_get_key = map.copy(source).get(1)
wrong_contains_key = map.copy(source).contains(1)
bad_size_arity = map.copy(source).size(1)
put_result = map.copy(source).put("key", 1.0)
clear_result = map.copy(source).clear()
remove_result = map.copy(source).remove("key")
put_all_result = map.copy(source).put_all(source)
keys_result = map.copy(source).keys()
values_result = map.copy(source).values()
wrong_receiver = map.copy(close).size()

plot(close)
