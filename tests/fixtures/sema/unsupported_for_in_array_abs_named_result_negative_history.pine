//@version=6
indicator("Unsupported for-in array.abs named result negative history")
source = array.from(-2.0, 1.0)
values = array.abs(id = source)
length = for value in values
    -1
plot(close[length])
