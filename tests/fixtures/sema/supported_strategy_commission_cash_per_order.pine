//@version=5
strategy("Commission cash per order", commission_type=strategy.commission.cash_per_order, commission_value=1.5)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=2)
if bar_index == 2
    strategy.close("L")
plot(strategy.closedtrades.commission(0))
