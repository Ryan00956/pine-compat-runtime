//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.unshift mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
array.unshift(points, Point.new(high))
plot(close)
