//@version=6
indicator("Unsupported for-in matrix.eigenvectors result negative history")
source = matrix.new<float>(2, 2, 1.0)
values = matrix.eigenvectors(source)
length = for row in values
    -1
plot(close[length])
