//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array sort_indices bool field")

values = array.from(lib.BoolOffset.new(close > open))
indices = array.sort_indices(values, order.ascending, "value")
plot(close)
