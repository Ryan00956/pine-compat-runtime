//@version=5
indicator("Unsupported user method duplicate named arg")
type Point
    float x

method shift(Point p, float delta) => p.x + delta
p = Point.new(close)
plot(p.shift(delta=open, delta=high))
