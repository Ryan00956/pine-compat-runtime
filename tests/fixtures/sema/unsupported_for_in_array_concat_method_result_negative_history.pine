//@version=6
indicator("Unsupported for-in array.concat method result negative history")
left = array.new<int>(0)
right = array.new<int>(1, 0)
values = left.concat(right)
length = for value in values
    -1
plot(close[length])
