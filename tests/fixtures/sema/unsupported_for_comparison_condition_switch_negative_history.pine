//@version=6
indicator("Unsupported for comparison condition switch negative history")
fast = for i = 0 to 0
    3
enabled = for i = 0 to 0
    true
slow = 2
length = switch
    fast > slow and enabled => -1
    => 1
plot(close[length])
