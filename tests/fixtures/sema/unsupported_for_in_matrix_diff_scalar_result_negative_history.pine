//@version=6
indicator("Unsupported for-in matrix.diff scalar result negative history")
left = matrix.new<float>(1, 0, 0)
values = matrix.diff(left, close)
length = for row in values
    -1
plot(close[length])
