//@version=6
indicator("Unsupported for-in matrix.mult array pair result negative history")
values = matrix.mult(array.from(1, 2), array.from(3, 4))
length = for value in values
    -1
plot(close[length])
