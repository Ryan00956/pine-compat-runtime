//@version=6
indicator("Supported empty matrix.diff scalar for-in result negative body history")
left = matrix.new<float>(0, 2, 0)
values = matrix.diff(left, close)
length = for row in values
    -1
plot(close[length])
