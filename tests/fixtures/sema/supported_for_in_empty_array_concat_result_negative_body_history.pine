//@version=6
indicator("Supported for-in empty array.concat result negative body history")
left = array.new<int>(0)
right = array.new<int>(0)
values = array.concat(left, right)
length = for value in values
    -1
plot(close[length])
