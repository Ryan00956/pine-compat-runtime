//@version=6
indicator("Unsupported bool cast return qualifier")

input_source = input.bool(true, "Input source")
simple_source = timeframe.isdaily
series_source = close > open

plot(ta.tr(bool(input_source)))
plot(ta.tr(bool(simple_source)))
plot(ta.alma(close, 4, 0.85, 6, bool(series_source)))
