//@version=6
indicator("Unsupported named const color comparison ternary negative history")
left = color.red
right = color.blue
use_short = left != right
length = use_short ? -1 : 1
plot(close[length])
