//@version=6
indicator("Unsupported for-in array.concat left result negative history")
left = array.new<int>(1, 0)
right = array.new<int>(0)
values = array.concat(left, right)
length = for value in values
    -1
plot(close[length])
