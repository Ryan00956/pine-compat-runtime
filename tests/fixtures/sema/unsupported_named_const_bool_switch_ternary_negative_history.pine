//@version=6
indicator("Unsupported named const bool switch ternary negative history")
mode = 2
use_short = switch mode
    1 => false
    2 =>
        true
    => false
length = use_short ? -1 : 1
plot(close[length])
