//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.fill mixed identity")

type Point
    float x

points = array.from(lib.Point.new(close))
array.fill(points, Point.new(high))
plot(close)
