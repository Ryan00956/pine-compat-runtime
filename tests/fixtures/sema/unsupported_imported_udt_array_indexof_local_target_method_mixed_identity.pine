//@version=5
import user/udt/1 as lib
indicator("Unsupported imported UDT array.indexof local target method mixed identity")

type Point
    float x

points = array.from(Point.new(close))
plot(points.indexof(lib.Point.new(high)))
