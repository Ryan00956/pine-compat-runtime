//@version=6
indicator("Supported const switch expression reassignment qualifier")
length = input.int(2, "Length")
_ = switch
    false =>
        length := bar_index
        1
    =>
        0
plot(ta.ema(close, length))
