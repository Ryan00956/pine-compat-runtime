//@version=5
import user/udt_side_effect/1 as lib
indicator("Unsupported imported method field mutation")

p = lib.Point.new(close)
other = lib.Point.new(open)
receiver = p.mutateReceiver()
param = p.mutateParam(other)

plot(receiver.x)
plot(param.x)
