//@version=6
indicator("Supported empty matrix.diff for-in result negative body history")
left = matrix.new<float>(0, 2, 0)
right = matrix.new<float>(0, 2, 0)
values = matrix.diff(left, right)
length = for row in values
    -1
plot(close[length])
