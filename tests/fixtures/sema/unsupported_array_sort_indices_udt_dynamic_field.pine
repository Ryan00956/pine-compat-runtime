//@version=5
indicator("Unsupported array sort_indices UDT dynamic field")
type Point
    float x

field = close > open ? "x" : "missing"
values = array.from(Point.new(close))
indices = array.sort_indices(values, order.ascending, field)
plot(close)
