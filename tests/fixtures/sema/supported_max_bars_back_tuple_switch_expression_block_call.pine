//@version=5
indicator("Supported max bars back tuple switch expression block call")
src = close
[branch] = switch
    close > open =>
        base = 5
        length = base + 5
        max_bars_back(src, length)
        [1]
    => [0]
plot(src[bar_index] + branch)
