//@version=6
indicator("Unsupported for bool selector switch local alias negative history")
key = for i = 0 to 0
    value = true
    value
length = switch key
    true => -1
    => 1
plot(close[length])
