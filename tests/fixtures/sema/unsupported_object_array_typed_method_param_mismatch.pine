//@version=5
indicator("Unsupported object array typed method param mismatch")

type Tool
    float base

method firstLineX(Tool tool, line[] values) =>
    tool.base + line.get_x1(array.get(values, 0))

tool = Tool.new(close)
values = array.new_label()
plot(tool.firstLineX(values))
