//@version=6
indicator("Unsupported builtin namespace array call-result reads")

float_matrix = matrix.new<float>(1, 1, 0.0)
mult_vector = array.from(2.0)
prices = map.new<string, float>()
prices.put("open", 1.0)
flags = map.new<string, bool>()
flags.put("open", true)

split_bad_index = str.split("a,b", ",").get("bad")
split_pop = str.split("a,b", ",").pop()
split_push = str.split("a,b", ",").push("c")
pivot_bad_index = ta.pivot_point_levels("Traditional", true, true).get("bad")
pivot_clear = ta.pivot_point_levels("Traditional", true, true).clear()
matrix_row_bad_index = matrix.row(float_matrix, "bad").size()
matrix_col_bad_index = matrix.col(float_matrix, "bad").first()
matrix_row_bad_get = matrix.row(float_matrix, 0).get("bad")
matrix_eigen_bad_get = matrix.eigenvalues(float_matrix).get("bad")
matrix_eigen_reverse = matrix.eigenvalues(float_matrix).reverse()
map_keys_bad_get = map.keys(prices).get("bad")
map_values_bad_get = map.values(prices).get("bad")
map_keys_remove = map.keys(prices).remove(0)
map_values_set = map.values(prices).set(0, 2.0)
mult_bad_get = matrix.mult(float_matrix, mult_vector).get("bad")
mult_pop = matrix.mult(float_matrix, mult_vector).pop()
mult_reverse = matrix.mult(mult_vector, float_matrix).copy().reverse()
split_includes_wrong_type = str.split("a,b", ",").includes(1)
pivot_includes_wrong_type = ta.pivot_point_levels("Traditional", true, true).includes("bad")
matrix_row_includes_wrong_type = matrix.row(float_matrix, 0).includes("bad")
map_keys_includes_wrong_type = map.keys(prices).includes(1)
map_values_includes_wrong_type = map.values(prices).includes("bad")
mult_includes_wrong_type = matrix.mult(float_matrix, mult_vector).includes("bad")
includes_extra_arg = str.split("a,b", ",").includes("a", "b")
split_indexof_wrong_type = str.split("a,b", ",").indexof(1)
pivot_indexof_wrong_type = ta.pivot_point_levels("Traditional", true, true).indexof("bad")
matrix_row_indexof_wrong_type = matrix.row(float_matrix, 0).indexof("bad")
map_keys_indexof_wrong_type = map.keys(prices).indexof(1)
map_values_indexof_wrong_type = map.values(prices).indexof("bad")
mult_indexof_wrong_type = matrix.mult(float_matrix, mult_vector).indexof("bad")
indexof_extra_arg = str.split("a,b", ",").indexof("a", "b")
split_lastindexof_wrong_type = str.split("a,b", ",").lastindexof(1)
pivot_lastindexof_wrong_type = ta.pivot_point_levels("Traditional", true, true).lastindexof("bad")
matrix_row_lastindexof_wrong_type = matrix.row(float_matrix, 0).lastindexof("bad")
map_keys_lastindexof_wrong_type = map.keys(prices).lastindexof(1)
map_values_lastindexof_wrong_type = map.values(prices).lastindexof("bad")
mult_lastindexof_wrong_type = matrix.mult(float_matrix, mult_vector).lastindexof("bad")
lastindexof_extra_arg = str.split("a,b", ",").lastindexof("a", "b")
split_binary_search = str.split("a,b", ",").binary_search("a")
matrix_bool_binary_search = matrix.row(matrix.new<bool>(1, 1, true), 0).binary_search(true)
matrix_string_binary_search = matrix.row(matrix.new<string>(1, 1, "a"), 0).binary_search("a")
matrix_color_binary_search = matrix.row(matrix.new<color>(1, 1, color.red), 0).binary_search(color.red)
map_bool_binary_search = map.values(flags).binary_search(true)
map_string_binary_search = map.keys(prices).binary_search("a")
numeric_binary_search_wrong_value = matrix.row(float_matrix, 0).binary_search("bad")
binary_search_extra_arg = matrix.mult(float_matrix, mult_vector).binary_search(1.0, 2.0)
split_binary_search_leftmost = str.split("a,b", ",").binary_search_leftmost("a")
matrix_bool_binary_search_leftmost = matrix.row(matrix.new<bool>(1, 1, true), 0).binary_search_leftmost(true)
matrix_string_binary_search_leftmost = matrix.row(matrix.new<string>(1, 1, "a"), 0).binary_search_leftmost("a")
matrix_color_binary_search_leftmost = matrix.row(matrix.new<color>(1, 1, color.red), 0).binary_search_leftmost(color.red)
map_bool_binary_search_leftmost = map.values(flags).binary_search_leftmost(true)
map_string_binary_search_leftmost = map.keys(prices).binary_search_leftmost("a")
numeric_binary_search_leftmost_wrong_value = matrix.row(float_matrix, 0).binary_search_leftmost("bad")
binary_search_leftmost_extra_arg = matrix.mult(float_matrix, mult_vector).binary_search_leftmost(1.0, 2.0)
split_binary_search_rightmost = str.split("a,b", ",").binary_search_rightmost("a")
matrix_bool_binary_search_rightmost = matrix.row(matrix.new<bool>(1, 1, true), 0).binary_search_rightmost(true)
matrix_string_binary_search_rightmost = matrix.row(matrix.new<string>(1, 1, "a"), 0).binary_search_rightmost("a")
matrix_color_binary_search_rightmost = matrix.row(matrix.new<color>(1, 1, color.red), 0).binary_search_rightmost(color.red)
map_bool_binary_search_rightmost = map.values(flags).binary_search_rightmost(true)
map_string_binary_search_rightmost = map.keys(prices).binary_search_rightmost("a")
numeric_binary_search_rightmost_wrong_value = matrix.row(float_matrix, 0).binary_search_rightmost("bad")
binary_search_rightmost_extra_arg = matrix.mult(float_matrix, mult_vector).binary_search_rightmost(1.0, 2.0)
split_abs = str.split("a,b", ",").abs()
matrix_bool_abs = matrix.row(matrix.new<bool>(1, 1, true), 0).abs()
matrix_string_abs = matrix.row(matrix.new<string>(1, 1, "a"), 0).abs()
matrix_color_abs = matrix.row(matrix.new<color>(1, 1, color.red), 0).abs()
map_bool_abs = map.values(flags).abs()
map_string_abs = map.keys(prices).abs()
namespace_call_result_abs_extra_arg = matrix.mult(float_matrix, mult_vector).abs(1.0)
split_min = str.split("a,b", ",").min()
matrix_bool_min = matrix.row(matrix.new<bool>(1, 1, true), 0).min()
matrix_string_min = matrix.row(matrix.new<string>(1, 1, "a"), 0).min()
matrix_color_min = matrix.row(matrix.new<color>(1, 1, color.red), 0).min()
map_bool_min = map.values(flags).min()
map_string_min = map.keys(prices).min()
namespace_call_result_min_float_nth = matrix.mult(float_matrix, mult_vector).min(1.5)
namespace_call_result_min_string_nth = matrix.row(float_matrix, 0).min(nth="1")
namespace_call_result_min_extra_arg = matrix.mult(float_matrix, mult_vector).min(0, 1)
split_max = str.split("a,b", ",").max()
matrix_bool_max = matrix.row(matrix.new<bool>(1, 1, true), 0).max()
matrix_string_max = matrix.row(matrix.new<string>(1, 1, "a"), 0).max()
matrix_color_max = matrix.row(matrix.new<color>(1, 1, color.red), 0).max()
map_bool_max = map.values(flags).max()
map_string_max = map.keys(prices).max()
namespace_call_result_max_float_nth = matrix.mult(float_matrix, mult_vector).max(1.5)
namespace_call_result_max_string_nth = matrix.row(float_matrix, 0).max(nth="1")
namespace_call_result_max_extra_arg = matrix.mult(float_matrix, mult_vector).max(0, 1)
split_sum = str.split("a,b", ",").sum()
matrix_bool_sum = matrix.row(matrix.new<bool>(1, 1, true), 0).sum()
matrix_string_sum = matrix.row(matrix.new<string>(1, 1, "a"), 0).sum()
matrix_color_sum = matrix.row(matrix.new<color>(1, 1, color.red), 0).sum()
map_bool_sum = map.values(flags).sum()
map_string_sum = map.keys(prices).sum()
namespace_call_result_sum_extra_arg = matrix.mult(float_matrix, mult_vector).sum(0)
split_avg = str.split("a,b", ",").avg()
matrix_bool_avg = matrix.row(matrix.new<bool>(1, 1, true), 0).avg()
matrix_string_avg = matrix.row(matrix.new<string>(1, 1, "a"), 0).avg()
matrix_color_avg = matrix.row(matrix.new<color>(1, 1, color.red), 0).avg()
map_bool_avg = map.values(flags).avg()
map_string_avg = map.keys(prices).avg()
namespace_call_result_avg_extra_arg = matrix.mult(float_matrix, mult_vector).avg(0)
split_range = str.split("a,b", ",").range()
matrix_bool_range = matrix.row(matrix.new<bool>(1, 1, true), 0).range()
matrix_string_range = matrix.row(matrix.new<string>(1, 1, "a"), 0).range()
matrix_color_range = matrix.row(matrix.new<color>(1, 1, color.red), 0).range()
map_bool_range = map.values(flags).range()
map_string_range = map.keys(prices).range()
namespace_call_result_range_extra_arg = matrix.mult(float_matrix, mult_vector).range(0)
split_median = str.split("a,b", ",").median()
matrix_bool_median = matrix.row(matrix.new<bool>(1, 1, true), 0).median()
matrix_string_median = matrix.row(matrix.new<string>(1, 1, "a"), 0).median()
matrix_color_median = matrix.row(matrix.new<color>(1, 1, color.red), 0).median()
map_bool_median = map.values(flags).median()
map_string_median = map.keys(prices).median()
namespace_call_result_median_extra_arg = matrix.mult(float_matrix, mult_vector).median(0)
split_mode = str.split("a,b", ",").mode()
matrix_bool_mode = matrix.row(matrix.new<bool>(1, 1, true), 0).mode()
matrix_string_mode = matrix.row(matrix.new<string>(1, 1, "a"), 0).mode()
matrix_color_mode = matrix.row(matrix.new<color>(1, 1, color.red), 0).mode()
map_bool_mode = map.values(flags).mode()
map_string_mode = map.keys(prices).mode()
namespace_call_result_mode_extra_arg = matrix.mult(float_matrix, mult_vector).mode(0)
split_percentile_nearest = str.split("a,b", ",").percentile_nearest_rank(50)
matrix_bool_percentile_nearest = matrix.row(matrix.new<bool>(1, 1, true), 0).percentile_nearest_rank(50)
matrix_string_percentile_nearest = matrix.row(matrix.new<string>(1, 1, "a"), 0).percentile_nearest_rank(50)
matrix_color_percentile_nearest = matrix.row(matrix.new<color>(1, 1, color.red), 0).percentile_nearest_rank(50)
map_bool_percentile_nearest = map.values(flags).percentile_nearest_rank(50)
map_string_percentile_nearest = map.keys(prices).percentile_nearest_rank(50)
namespace_call_result_percentile_nearest_missing_arg = matrix.mult(float_matrix, mult_vector).percentile_nearest_rank()
namespace_call_result_percentile_nearest_bad_percentage = matrix.mult(float_matrix, mult_vector).percentile_nearest_rank("50")
namespace_call_result_percentile_nearest_extra_arg = matrix.mult(float_matrix, mult_vector).percentile_nearest_rank(50, 0)
split_percentile_linear = str.split("a,b", ",").percentile_linear_interpolation(50)
matrix_bool_percentile_linear = matrix.row(matrix.new<bool>(1, 1, true), 0).percentile_linear_interpolation(50)
matrix_string_percentile_linear = matrix.row(matrix.new<string>(1, 1, "a"), 0).percentile_linear_interpolation(50)
matrix_color_percentile_linear = matrix.row(matrix.new<color>(1, 1, color.red), 0).percentile_linear_interpolation(50)
map_bool_percentile_linear = map.values(flags).percentile_linear_interpolation(50)
map_string_percentile_linear = map.keys(prices).percentile_linear_interpolation(50)
namespace_call_result_percentile_linear_missing_arg = matrix.mult(float_matrix, mult_vector).percentile_linear_interpolation()
namespace_call_result_percentile_linear_bad_percentage = matrix.mult(float_matrix, mult_vector).percentile_linear_interpolation("50")
namespace_call_result_percentile_linear_extra_arg = matrix.mult(float_matrix, mult_vector).percentile_linear_interpolation(50, 0)
split_percentrank = str.split("a,b", ",").percentrank(0)
matrix_bool_percentrank = matrix.row(matrix.new<bool>(1, 1, true), 0).percentrank(0)
matrix_string_percentrank = matrix.row(matrix.new<string>(1, 1, "a"), 0).percentrank(0)
matrix_color_percentrank = matrix.row(matrix.new<color>(1, 1, color.red), 0).percentrank(0)
map_bool_percentrank = map.values(flags).percentrank(0)
map_string_percentrank = map.keys(prices).percentrank(0)
namespace_call_result_percentrank_missing_arg = matrix.mult(float_matrix, mult_vector).percentrank()
namespace_call_result_percentrank_bad_index = matrix.mult(float_matrix, mult_vector).percentrank("0")
namespace_call_result_percentrank_series_index = matrix.mult(float_matrix, mult_vector).percentrank(bar_index)
namespace_call_result_percentrank_extra_arg = matrix.mult(float_matrix, mult_vector).percentrank(0, 1)
split_covariance = str.split("a,b", ",").covariance(array.from(1, 2))
matrix_bool_covariance = matrix.row(matrix.new<bool>(1, 1, true), 0).covariance(array.from(1))
matrix_string_covariance = matrix.row(matrix.new<string>(1, 1, "a"), 0).covariance(array.from(1))
matrix_color_covariance = matrix.row(matrix.new<color>(1, 1, color.red), 0).covariance(array.from(1))
map_bool_covariance = map.values(flags).covariance(array.from(1, 2))
map_string_covariance = map.keys(prices).covariance(array.from(1, 2))
namespace_call_result_covariance_bad_id2 = matrix.mult(float_matrix, mult_vector).covariance(array.from("a", "b"))
namespace_call_result_covariance_missing_arg = matrix.mult(float_matrix, mult_vector).covariance()
namespace_call_result_covariance_bad_biased = matrix.mult(float_matrix, mult_vector).covariance(array.from(1.0, 2.0), close)
namespace_call_result_covariance_extra_arg = matrix.mult(float_matrix, mult_vector).covariance(array.from(1.0, 2.0), true, false)
split_standardize = str.split("a,b", ",").standardize()
matrix_bool_standardize = matrix.row(matrix.new<bool>(1, 1, true), 0).standardize()
matrix_string_standardize = matrix.row(matrix.new<string>(1, 1, "a"), 0).standardize()
matrix_color_standardize = matrix.row(matrix.new<color>(1, 1, color.red), 0).standardize()
map_bool_standardize = map.values(flags).standardize()
map_string_standardize = map.keys(prices).standardize()
namespace_call_result_standardize_extra_arg = matrix.mult(float_matrix, mult_vector).standardize(0)
split_variance = str.split("a,b", ",").variance()
matrix_bool_variance = matrix.row(matrix.new<bool>(1, 1, true), 0).variance()
matrix_string_variance = matrix.row(matrix.new<string>(1, 1, "a"), 0).variance()
matrix_color_variance = matrix.row(matrix.new<color>(1, 1, color.red), 0).variance()
map_bool_variance = map.values(flags).variance()
map_string_variance = map.keys(prices).variance()
namespace_call_result_variance_bad_biased = matrix.mult(float_matrix, mult_vector).variance(close)
namespace_call_result_variance_extra_arg = matrix.mult(float_matrix, mult_vector).variance(true, false)
split_stdev = str.split("a,b", ",").stdev()
matrix_bool_stdev = matrix.row(matrix.new<bool>(1, 1, true), 0).stdev()
matrix_string_stdev = matrix.row(matrix.new<string>(1, 1, "a"), 0).stdev()
matrix_color_stdev = matrix.row(matrix.new<color>(1, 1, color.red), 0).stdev()
map_bool_stdev = map.values(flags).stdev()
map_string_stdev = map.keys(prices).stdev()
namespace_call_result_stdev_bad_biased = matrix.mult(float_matrix, mult_vector).stdev(close)
namespace_call_result_stdev_extra_arg = matrix.mult(float_matrix, mult_vector).stdev(true, false)
matrix_bool_sort_indices = matrix.row(matrix.new<bool>(1, 1, true), 0).sort_indices()
matrix_color_sort_indices = matrix.row(matrix.new<color>(1, 1, color.red), 0).sort_indices()
map_bool_sort_indices = map.values(flags).sort_indices()
map_color_sort_indices = map.keys(map.new<color, int>()).sort_indices()
namespace_call_result_sort_indices_bad_order = matrix.mult(float_matrix, mult_vector).sort_indices(close)
namespace_call_result_sort_indices_extra_arg = matrix.mult(float_matrix, mult_vector).sort_indices(order.ascending, "unused")
split_every = str.split("a,b", ",").every()
matrix_string_every = matrix.row(matrix.new<string>(1, 1, "a"), 0).every()
matrix_color_every = matrix.row(matrix.new<color>(1, 1, color.red), 0).every()
map_string_every = map.keys(prices).every()
map_color_every = map.keys(map.new<color, int>()).every()
namespace_call_result_every_extra_arg = matrix.mult(float_matrix, mult_vector).every(true)
