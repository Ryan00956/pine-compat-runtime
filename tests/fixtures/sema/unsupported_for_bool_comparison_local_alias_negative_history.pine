//@version=6
indicator("Unsupported for bool comparison local alias negative history")
left = for i = 0 to 0
    value = true
    value
right = false
length = left != right ? -1 : 1
plot(close[length])
