//@version=6
indicator("Supported empty array.standardize for-in result negative body history")
source = array.new_float(0)
values = array.standardize(source)
length = for value in values
    -1
plot(close[length])
