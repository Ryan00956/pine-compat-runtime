//@version=6
indicator("Supported empty matrix.mult left array result negative body history")
right = matrix.new<float>(2, 0, 0)
values = matrix.mult(array.from(1, 2), right)
length = for value in values
    -1
plot(close[length])
