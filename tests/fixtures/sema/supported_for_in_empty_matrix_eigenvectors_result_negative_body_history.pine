//@version=6
indicator("Supported empty matrix.eigenvectors for-in result negative body history")
source = matrix.new<float>(0, 0, 0)
values = matrix.eigenvectors(source)
length = for row in values
    -1
plot(close[length])
