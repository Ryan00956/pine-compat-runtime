//@version=5
indicator("Unsupported array sort_indices UDT dynamic field method")
type Point
    float x

field = close > open ? "x" : "missing"
values = array.from(Point.new(close))
indices = values.sort_indices(order.ascending, field)
plot(close)
