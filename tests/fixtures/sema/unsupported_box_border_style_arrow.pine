//@version=5
indicator("Unsupported box border arrow styles")
created = box.new(bar_index, high, bar_index + 1, low, border_style=line.style_arrow_left)
box.set_border_style(created, line.style_arrow_right)
plot(close)
