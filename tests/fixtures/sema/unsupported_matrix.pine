//@version=6
indicator("Unsupported matrix concat template")

float_values = matrix.new<float>(1, 1, 1)
int_values = matrix.new<int>(1, 1, 1)
value = matrix.concat(float_values, int_values)
plot(value.rows())
