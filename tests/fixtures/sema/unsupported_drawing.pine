//@version=5
indicator("Unsupported drawing")
label.set_point(na, na)
line.get_price(na, bar_index)
box.set_text_formatting(na, "text.format_bold")
table.cell_set_tooltip(na, 0, 0, "tip")
polyline.new(na)
