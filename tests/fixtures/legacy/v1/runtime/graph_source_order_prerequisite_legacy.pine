study("Implicit v1 graph source-order prerequisite")
length = input(2)
delta = close - close[length]
trend = delta > 0 ? 1 : delta < 0 ? -1 : nz(trend[1])
direction = rising(close, length) ? 1 : falling(close, length) ? -1 : 0
plot(trend, title="trend")
plot(direction, title="direction")
