//@version=6
indicator("Chart-inherited resolution", overlay=false, max_boxes_count=75)
average = ta.sma(close, 2)
chartContext = timeframe.period == "5" and syminfo.tickerid == "TEST:CHART"
plot(average)
plot(chartContext ? close : 0)
