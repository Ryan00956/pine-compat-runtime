//@version=4
study("Legacy security time_close")
plot(security(syminfo.tickerid, timeframe.period, time_close("D")), "close")
plot(security(syminfo.tickerid, timeframe.period, time(timeframe="D")), "named")
