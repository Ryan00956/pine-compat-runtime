//@version=6
indicator("Canonical integer division")
length = input.int(5, title="Length")
lag = int(length / 2)
weighted(source, window) => ta.wma(source, window)
plot(ta.wma(close, lag))
plot(weighted(close, int(length / 2)))
plot(int(length / 2))
plot(close[lag])
plot(close[int(5 / 2)])
