indicator("Profile dynamic series history statement switch miss", max_bars_back=10)
src = close
switch
    bar_index >= 0 =>
        length = 2
        max_bars_back(src, length)
    =>
        high
offset = bar_index == 0 ? 0 : 3
plot(src[offset])
