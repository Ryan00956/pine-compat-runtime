//@version=5
import user/udt/1 as lib
indicator("Profile dynamic series history nested pure user method imported named direct nested UDT arg expression max_bars_back miss", max_bars_back=10)
p = lib.Point.new(open)
src = p.otherNestedSourceNamed(delta=1, other=lib.Wrapper.new(lib.Point.new(close)))
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(p.otherNestedSourceNamed(other=lib.Wrapper.new(lib.Point.new(close)), delta=1)[offset])
