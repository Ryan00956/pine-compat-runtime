//@version=5
indicator("Profile dynamic series history named variadic math call miss", max_bars_back=10)
src = math.max(a=close, b=open)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(math.max(b=open, a=close)[offset])
