//@version=5
indicator("Profile dynamic series history named pure math call miss", max_bars_back=10)
src = math.pow(base=close, exponent=open)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(math.pow(exponent=open, base=close)[offset])
