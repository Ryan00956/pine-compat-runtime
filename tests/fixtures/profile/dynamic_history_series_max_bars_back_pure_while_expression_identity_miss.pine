indicator("Profile dynamic series history pure while expression identity miss", max_bars_back=10)
offset = bar_index == 0 ? 0 : 3
src = while false
    base = close
    base
same = while false
    base = close
    base
max_bars_back(src, 2)
plot(same[offset])
