indicator("Profile dynamic series history pure UDF UDT arg field max_bars_back miss", max_bars_back=10)
type Point
    float x
source(p) => p.x + 1
p = Point.new(close)
src = source(p)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(source(p)[offset])
