//@version=5
indicator("Profile dynamic series history ternary expression miss", max_bars_back=10)
src = close > open ? close : open
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot((close > open ? close : open)[offset])
