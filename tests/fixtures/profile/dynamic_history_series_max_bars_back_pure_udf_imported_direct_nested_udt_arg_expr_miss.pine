//@version=5
import user/udt/1 as lib
indicator("Profile dynamic series history pure UDF imported direct nested UDT arg expression max_bars_back miss", max_bars_back=10)
source(w) => w.nested.x + 1
src = source(lib.Wrapper.new(lib.Point.new(close)))
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(source(lib.Wrapper.new(lib.Point.new(close)))[offset])
