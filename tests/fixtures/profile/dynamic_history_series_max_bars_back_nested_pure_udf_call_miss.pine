//@version=5
indicator("Profile dynamic series history nested pure UDF max_bars_back miss", max_bars_back=10)
adjust(value) => value + 1
source(a) => adjust(a)
src = source(close)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(source(a=close)[offset])
