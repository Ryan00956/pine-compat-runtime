indicator("Profile dynamic series history for statement miss", max_bars_back=10)
src = close
for i = 0 to 0
    length = 2
    max_bars_back(src, length)
offset = bar_index == 0 ? 0 : 3
plot(src[offset])
