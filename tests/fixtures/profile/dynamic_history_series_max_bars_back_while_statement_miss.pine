indicator("Profile dynamic series history while statement miss", max_bars_back=10)
src = close
i = 0
while i < 1
    i := i + 1
    length = 2
    max_bars_back(src, length)
offset = bar_index == 0 ? 0 : 3
plot(src[offset])
