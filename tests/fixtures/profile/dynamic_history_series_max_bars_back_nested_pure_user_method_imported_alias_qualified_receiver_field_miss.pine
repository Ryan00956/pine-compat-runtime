//@version=5
import user/udt/1 as lib
indicator("Profile dynamic series history nested pure user method imported alias-qualified receiver field max_bars_back miss", max_bars_back=10)
p = lib.Point.new(close)
w = lib.Wrapper.new(p)
src = lib.nestedSource(w)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(lib.nestedSource(w)[offset])
