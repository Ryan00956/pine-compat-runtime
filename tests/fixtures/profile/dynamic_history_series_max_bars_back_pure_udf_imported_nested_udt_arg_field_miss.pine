//@version=5
import user/udt/1 as lib
indicator("Profile dynamic series history pure UDF imported nested UDT arg field max_bars_back miss", max_bars_back=10)
source(w) => w.nested.x + 1
p = lib.Point.new(close)
w = lib.Wrapper.new(p)
src = source(w)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(source(w)[offset])
