indicator("Profile dynamic series history nested UDT field expression max_bars_back miss", max_bars_back=10)
type Point
    float x
type Wrapper
    Point inner
p = Point.new(close)
w = Wrapper.new(p)
src = w.inner.x + 1
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot((w.inner.x + 1)[offset])
