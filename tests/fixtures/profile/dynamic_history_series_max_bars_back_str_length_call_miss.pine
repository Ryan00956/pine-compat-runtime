indicator("Profile dynamic series history str.length call max_bars_back miss", max_bars_back=10)
src = str.length(close > open ? "long" : "s")
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(str.length(close > open ? "long" : "s")[offset])
