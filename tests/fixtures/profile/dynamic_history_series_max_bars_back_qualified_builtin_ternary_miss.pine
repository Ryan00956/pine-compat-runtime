//@version=5
indicator("Profile dynamic series history qualified builtin ternary miss", max_bars_back=10)
src = barstate.isconfirmed ? close : open
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot((barstate.isconfirmed ? close : open)[offset])
