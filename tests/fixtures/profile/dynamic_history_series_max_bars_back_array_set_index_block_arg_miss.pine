//@version=5
indicator("Profile dynamic series history array.set index block arg miss", max_bars_back=10)
src = close
offset = bar_index == 0 ? 0 : 3
values = array.new_float(1, 0.0)
values.set(switch
    true =>
        length = 2
        max_bars_back(src, length)
        0
    => 0
, src[offset])
plot(values.get(0))
