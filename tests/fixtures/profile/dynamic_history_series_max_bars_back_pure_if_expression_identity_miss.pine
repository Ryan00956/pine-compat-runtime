//@version=5
indicator("Profile dynamic series history pure if expression identity miss", max_bars_back=10)
src = if close > open
    base = close
    base
else
    open
same = if close > open
    base = close
    base
else
    open
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(same[offset])
