//@version=5
indicator("Profile dynamic series history UDT field expression max_bars_back miss", max_bars_back=10)
type Point
    float x
p = Point.new(close)
src = p.x + 1
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot((p.x + 1)[offset])
