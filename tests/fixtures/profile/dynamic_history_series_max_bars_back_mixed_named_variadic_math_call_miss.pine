indicator("Profile dynamic series history mixed named variadic math call miss", max_bars_back=10)
src = math.max(close, b=open)
max_bars_back(src, 2)
offset = bar_index == 0 ? 0 : 3
plot(math.max(a=close, b=open)[offset])
