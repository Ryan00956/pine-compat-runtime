//@version=5
import user/udt/1 as lib
indicator("Dynamic negative history offset imported UDT field method nested passthrough")

box = lib.OffsetBox.new(lib.Offset.new(bar_index == 0 ? 0 : -1))
same = box.sameBox()
plot(close[same.offset.value])
