//@version=6
indicator("Map for-in put size change")

values = map.new<string, float>()
values.put("open", open)
values.put("close", close)

for [key, value] in values
    values.put("next", value)

plot(values.size())
