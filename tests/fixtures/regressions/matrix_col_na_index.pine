//@version=6
indicator("Matrix column extraction na index")

values = matrix.new<float>(2, 2, close)
missing_column = int(na)
column_values = matrix.col(values, missing_column)
plot(column_values.get(0))
