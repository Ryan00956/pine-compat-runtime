//@version=5
indicator("Dynamic negative history offset UDT field UDF passthrough")
type Settings
    int offset

sameSettings(Settings settings) => settings

settings = Settings.new(bar_index == 0 ? 0 : -1)
same = sameSettings(settings)
plot(close[same.offset])
