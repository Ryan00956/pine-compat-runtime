//@version=5
import user/udt/1 as lib
indicator("Dynamic negative history offset imported UDT field alias-qualified method nested return")

source = lib.Offset.new(0)
box = lib.makeBox(source, bar_index == 0 ? 0 : -1)
plot(close[box.offset.value])
